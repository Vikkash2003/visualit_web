import * as React from 'react';
import { Props as DeviceProps } from '../Device';
export declare type Props = Omit<Omit<DeviceProps, 'device'>, 'color'> & {
    orientation?: 'portrait' | 'landscape';
};
export default class IPhoneX extends React.Component<Props> {
    render(): JSX.Element;
}
