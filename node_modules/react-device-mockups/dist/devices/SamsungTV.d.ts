import * as React from 'react';
import { Props as DeviceProps } from '../Device';
export declare type Props = Omit<Omit<Omit<DeviceProps, 'device'>, 'color'>, 'orientation'>;
export default class SamsungTV extends React.Component<Props> {
    render(): JSX.Element;
}
